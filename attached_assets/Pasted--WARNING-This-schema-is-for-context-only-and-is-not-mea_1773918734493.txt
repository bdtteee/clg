-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.admin_actions_log (
  id integer NOT NULL DEFAULT nextval('admin_actions_log_id_seq'::regclass),
  admin_id uuid,
  application_id uuid,
  action_type text NOT NULL,
  details jsonb,
  action_timestamp timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT admin_actions_log_pkey PRIMARY KEY (id),
  CONSTRAINT admin_actions_log_admin_id_fkey FOREIGN KEY (admin_id) REFERENCES auth.users(id),
  CONSTRAINT admin_actions_log_application_id_fkey FOREIGN KEY (application_id) REFERENCES public.applications(id)
);
CREATE TABLE public.applications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  type text NOT NULL CHECK (type = ANY (ARRAY['grant'::text, 'loan'::text])),
  category text NOT NULL CHECK (category = ANY (ARRAY['personal'::text, 'business'::text])),
  full_name text NOT NULL,
  email text NOT NULL,
  phone_number text NOT NULL,
  country text NOT NULL,
  reason text NOT NULL,
  amount_requested numeric NOT NULL,
  business_name text,
  status text NOT NULL DEFAULT 'Pending Verification'::text,
  submission_date timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  pre_approved_amount numeric,
  approved_amount numeric,
  assigned_partner text,
  disbursement_date date,
  time_lapse_days integer,
  payout_method text,
  payout_details jsonb,
  verification_status text DEFAULT 'Pending'::text,
  id_document_url text,
  photo_id_url text,
  tax_document_url text,
  company_certificate_url text,
  currency text,
  rejection_reason text,
  service_fee numeric,
  service_fee_status text DEFAULT 'pending'::text,
  ssn text,
  sin text,
  national_id_number text,
  credit_score_range text,
  bank_details jsonb,
  review_notes text,
  CONSTRAINT applications_pkey PRIMARY KEY (id),
  CONSTRAINT applications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_profiles(id)
);
CREATE TABLE public.kyc_documents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  document_type text NOT NULL,
  file_url text,
  file_path text,
  status text NOT NULL DEFAULT 'Not Uploaded'::text,
  rejection_reason text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT kyc_documents_pkey PRIMARY KEY (id),
  CONSTRAINT kyc_documents_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_profiles(id)
);
CREATE TABLE public.manual_payments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  application_id uuid,
  user_name text NOT NULL,
  phone_number text NOT NULL,
  mpesa_confirmation_code text NOT NULL,
  amount_kes numeric NOT NULL,
  is_verified boolean NOT NULL DEFAULT false,
  verified_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT manual_payments_pkey PRIMARY KEY (id),
  CONSTRAINT manual_payments_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_profiles(id),
  CONSTRAINT manual_payments_application_id_fkey FOREIGN KEY (application_id) REFERENCES public.applications(id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  application_id uuid,
  message text NOT NULL,
  is_read boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  type text,
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_profiles(id),
  CONSTRAINT notifications_application_id_fkey FOREIGN KEY (application_id) REFERENCES public.applications(id)
);
CREATE TABLE public.partner_list (
  id integer NOT NULL DEFAULT nextval('partner_list_id_seq'::regclass),
  name text NOT NULL UNIQUE,
  contact_email text,
  services_offered text,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT partner_list_pkey PRIMARY KEY (id)
);
CREATE TABLE public.payments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  application_id uuid,
  user_id uuid NOT NULL,
  amount numeric NOT NULL,
  currency text NOT NULL,
  phone_number text NOT NULL,
  provider text NOT NULL DEFAULT 'M-PESA'::text,
  status text NOT NULL DEFAULT 'pending'::text,
  checkout_request_id text,
  provider_reference text,
  callback_payload jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  manual_payment_id uuid,
  exchange_rate_used numeric,
  amount_usd numeric,
  CONSTRAINT payments_pkey PRIMARY KEY (id),
  CONSTRAINT payments_application_id_fkey FOREIGN KEY (application_id) REFERENCES public.applications(id),
  CONSTRAINT payments_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_profiles(id),
  CONSTRAINT payments_manual_payment_id_fkey FOREIGN KEY (manual_payment_id) REFERENCES public.manual_payments(id)
);
CREATE TABLE public.user_profiles (
  id uuid NOT NULL,
  full_name text,
  phone_number text,
  phone_verified boolean DEFAULT false,
  country text,
  currency text DEFAULT 'USD'::text,
  payout_method text,
  payout_details jsonb,
  tax_number text,
  is_business_profile boolean DEFAULT false,
  business_name text,
  profile_verified_tick boolean DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  email text,
  avatar_url text,
  pre_approved_amount numeric,
  kyc_status text DEFAULT 'Not Submitted'::text,
  kyc_notes text,
  CONSTRAINT user_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT user_profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);